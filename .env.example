# Frontend: set VITE_API_URL to the Render backend URL for production
# (leave empty for local dev with Vite proxy)
VITE_API_URL=

# Backend: comma-separated allowed origins for CORS
# e.g. https://pharmaguard.vercel.app
CORS_ORIGIN=

# Backend URL (used by frontend dev server proxy)
BACKEND_URL=http://localhost:5000
